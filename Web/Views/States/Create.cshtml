@model Web.Models.Domain.State

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>State</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RunningElectionID" class="control-label"></label>
                <select asp-for="RunningElectionID" class ="form-control" asp-items="ViewBag.RunningElectionID"></select>
            </div>
            <div class="form-group">
                <label asp-for="ManagedElectionID" class="control-label"></label>
                <select asp-for="ManagedElectionID" class ="form-control" asp-items="ViewBag.ManagedElectionID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
